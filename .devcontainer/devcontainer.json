{
  "name": "Guided Discovery AI",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "pnpm install --frozen-lockfile && python -m pip install --disable-pip-version-check pytest==9.1.1 ruff==0.15.22 && pnpm exec playwright install --with-deps chromium",
  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker",
        "ms-python.python"
      ]
    }
  }
}
