# @guided-discovery/shared-types

Provider-neutral shared TypeScript contracts for identifiers, UTC timestamps, API envelopes, pagination metadata, request/trace context, permission and policy references, optimistic versions, and health responses.

This package contains no domain entities, validation behavior, persistence code, or authentication logic. It is the lowest-level M2.1 dependency and has no runtime dependencies.
