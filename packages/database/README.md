# @guided-discovery/database

Provider-neutral repository, pagination, transaction, unit-of-work, and migration interfaces.

This package contains no database client, SQL, Prisma schema, PostgreSQL entity, domain repository, migration, or connection implementation. Services implement these ports inside their own ownership boundary.
