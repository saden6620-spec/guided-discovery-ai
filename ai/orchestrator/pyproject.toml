[project]
name = "guided-discovery-ai-orchestrator"
version = "0.1.0"
description = "M1 placeholder AI Orchestrator service."
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.52.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
