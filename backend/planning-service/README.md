# Planning Service

M1 provides a launchable NestJS process, environment-based configuration, and a `GET /health` endpoint.

No planning behavior, authentication behavior, persistence, schemas, or migrations are implemented.
